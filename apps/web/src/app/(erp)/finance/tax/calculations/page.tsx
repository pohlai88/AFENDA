import { Card, CardContent, CardHeader, CardTitle } from "@afenda/ui";

/** Tax Calculations — placeholder. */
export default function TaxCalculationsPage() {
  return (
    <div className="max-w-5xl mx-auto px-6 py-8">
      <Card>
        <CardHeader>
          <CardTitle>Tax Calculations</CardTitle>
        </CardHeader>
        <CardContent>
          <p className="text-sm text-muted-foreground">
            Tax calculations and returns — coming soon.
          </p>
        </CardContent>
      </Card>
    </div>
  );
}
