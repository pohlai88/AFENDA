import { Card, CardContent, CardHeader, CardTitle } from "@afenda/ui";

/** Admin insights — placeholder. */
export default function InsightsPage() {
  return (
    <div className="max-w-5xl mx-auto px-6 py-8">
      <Card>
        <CardHeader>
          <CardTitle>OTel Insight Factory</CardTitle>
        </CardHeader>
        <CardContent>
          <p className="text-sm text-muted-foreground">
            Insights placeholder. Component examination in progress.
          </p>
        </CardContent>
      </Card>
    </div>
  );
}
